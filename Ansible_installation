SHORTCUT:
  wget -O - https://raw.githubusercontent.com/sakit333/ansible_insta/main/ans_insta.sh | bash
--------------------------------------------------------------------------------------------------------------------------------------------------------------------
ANSIBLE INSTALLATION COMMANDS:
--------------------------------------------------------------------------------------------------------------------------------------------------------------------
1. MASTER_NODE:
        
sudo yum update -y
sudo amazon-linux-extras install ansible2 -y
ansible --version
vi /etc/ansible/hosts
    [group_name]
    worker_node_private_ip
vi /etc/ansible/ansible.cfg
    - uncomment: inventory
    - uncomment: sudo-user
useradd ansible
passwd ansible
  - enter the password
  - confirm the password
visudo
  -Add: username(ansible) ALL=(ALL) NOPASSWD: ALL
vi /etc/ssh/sshd_config
  - permitrootuser
  - password authentication - yes
service sshd restart

--> ENTER INSIDE ANSIBLE USER AND PERORM THESE COMMANDS:

su - ansible
ssh-keygen
ssh-copy-id ansible@worker_node_private_ip
ansible [group_name] -m ping

NOTE: 
  - cd .ssh/ : stores private keys generated by ssh

--------------------------------------------------------------------------------------------------------------------------------------------------------------------

2. WORKER_NODE:

useradd ansible
passwd ansible
  - enter the password
  - confirm the password
visudo
  -Add: username(ansible) ALL=(ALL) NOPASSWD: ALL
vi /etc/ssh/sshd_config
  - permitrootuser
  - password authentication - yes
service sshd restart
